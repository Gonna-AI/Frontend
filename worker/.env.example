# Copy this to .env on the VPS and fill in real values. Never commit .env.
# DATABASE_URL: Supabase project Settings -> Database -> Connection string (use the
# "Session pooler" or direct connection URI; must include a password).
DATABASE_URL=postgresql://postgres:REPLACE_ME@db.xlzwfkgurrrspcdyqele.supabase.co:5432/postgres

# GROQ_API_KEY: from console.groq.com. Rotate this if it was ever pasted in plaintext.
GROQ_API_KEY=REPLACE_ME
GROQ_MODEL=llama-3.3-70b-versatile

EMBEDDING_MODEL=intfloat/multilingual-e5-small
PORT=8200
POLL_INTERVAL_SECONDS=3
